---
name: Bug report
about: Something in living-ledger did not do what the docs say
labels: bug
---

**What happened** (and what you expected)


**Output** (the hook or installer message, the digest line, or the test failure)

```
```

**The commit message involved**, if any (redact anything private)

```
```

**Environment**
- OS:
- `git --version`:
- `python3 --version`:
- living-ledger version (`git -C ~/.claude/skills/living-ledger log -1 --format=%h`):
- template version in the repo (`grep template-version .claude/ledger.conf`):
